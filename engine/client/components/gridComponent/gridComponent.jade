template(name="gridComponent")
  .igrid(class="{{wrapperClass}}")
    if headerTemplate
      .igrid-header
        +Template.dynamic(template=headerTemplate)
    .igrid-content
      each dataSource
        .item-wrapper(class="{{animationClass}}")
          +Template.dynamic(template=itemTemplate data=this)